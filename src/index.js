import Box from './components/Grid/Box'
import Container from './components/Grid/Container'
import Row from './components/Grid/Row'
import Col from './components/Grid/Col'
import Button from './components/Button'

export { Box, Button }

// TODO
/**
 * Finish Row (gutters)
 * Finish Col (flexbox + simple col)
 * Show all possible combinations in docs
 * Clean code
 * Write universal mixins !!!
 * Create typography components with docs
 * Create button components with docs
 * Create inputs (all types) + select box with docs
 * Design tokens
 * Create form component with validation and docs
 * Write JSON docs
 * Move code to the @qonsoll/react-design
 */

export default {
  Box,
  Container,
  Row,
  Col,
  Button
}
