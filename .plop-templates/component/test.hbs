import React from "react";
import { render } from "@testing-library/react";

import {{name}} from "./{{name}}.template";
import { {{name}}Props } from "./{{name}}.types";

describe("{{name}}", () => {
  let props: {{name}}Props;

  const renderComponent = () => render(<{{name}} {...props} />);
});