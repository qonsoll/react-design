import React from 'react'
import { {{name}}Props } from "./{{name}}.types";
import "./{{name}}.scss";
import styled from 'styled-components'

const {{name}}: React.FC<{{name}}Props> = ({ theme }) => (
  <div>
    {{name}}
  </div>
);

export default {{name}};